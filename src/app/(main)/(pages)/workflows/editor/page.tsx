import React from 'react'

type Props = {}

const Page = (props: Props) => {
  //CHALLENGE: If the user tries to access this route you should send them to their first workflow they have or create one or you can have your own behavior.
  return <div>This is just a page for now it is not implemented yet you can acces only through urls: http://localhost:3000/workflows/editor</div>
}

export default Page